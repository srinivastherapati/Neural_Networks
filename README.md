Name:Srinivas Therapati
Id:700755602

1.TensorFlow Tensor Operations

Creating and reshaping tensors using rando.uniform() method
Finding tensor rank and shape of tensor using rabk() and shape().
Transposing tensors for different dimensions using transpose().
Broadcasting smaller tensors to match larger ones and performing addition on them.

2.Loss Function Comparison
This project implements and compares Mean Squared Error (MSE) and Categorical Cross-Entropy (CCE) loss functions.

Computes loss for different model predictions.
Observes how slight changes in predictions affect loss values.
Visualizes loss comparisons using a bar chart with Matplotlib.

3.Neural Network Model: Adam vs. SGD
This project trains a neural network on the MNIST dataset using two different optimizers: Adam and SGD.

Compares training and validation accuracy trends.
Observes the impact of optimizers on model performance.
Visualizes accuracy comparisons using Matplotlib.

4.TensorFlow MNIST Example
This project trains a neural network on the MNIST dataset and logs the training process using TensorBoard.

Preprocesses and normalizes the dataset.
Defines and trains a simple fully connected neural network.
Logs training metrics (loss & accuracy) for visualization in TensorBoard.
